//Escriba una función que reciba dos enteros sin signo y devuelva el promedio
#include <stdio.h>

float funcion(int unsigned num,int unsigned num1){
  return (float) (num+num1)/2;
}

int main(){
    int n1;
    printf("Promedio: %2f",3,funcion(3,5));
    scanf("%i",&n1);
}
